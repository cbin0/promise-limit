{
  "name": "promise-parallel-limit",
  "version": "1.0.0",
  "description": "limit parallel count when use Promise.all",
  "main": "index.js",
  "scripts": {
    "test": "npm run build && mocha",
    "build": "babel src/ -d build"
  },
  "author": "cuibin",
  "babel": {
    "presets": [
      "es2015"
    ]
  },
  "license": "MIT",
  "dependencies": {
    "bluebird": "^3.4.7",
    "lodash": "^4.17.4"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.18.0",
    "babel-register": "^6.18.0"
  }
}
